# charts/core/values.yaml
replicaCount: 3

image:
  repository: juhyi/core-app
  tag: "1.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8002

containerPort: 8002

database:
  writableUrl: "postgresql://scott:tiger@localhost:5432/core_db"
  readonlyUrl: "postgresql://scott:tiger@localhost:5432/core_db"

deps:
  authUrl: "http://auth-app-service:8001"
